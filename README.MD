# HOW TO USE 

## import code base

```
git submodule update --init --recursive
```
## start the servers 

start & rebuild

```
docker-compose up --build
```

start 
```
docker-compose up
```

build 

```
docker-compose build
```